# Keep the dependency tree honest: a package that is not declared is not
# importable. pnpm's default, stated here so it survives a global override.
node-linker=isolated
strict-peer-dependencies=false
